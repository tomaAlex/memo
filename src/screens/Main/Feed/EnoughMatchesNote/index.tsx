import React from "react";
import { Text } from "react-native";

const EnoughMatchesNote = () => {
	return <Text style={{ top: "50%" }}>It is time you focus on your current matches 🔥</Text>;
};

export default React.memo(EnoughMatchesNote);
